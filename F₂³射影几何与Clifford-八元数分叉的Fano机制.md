# F₂³射影几何与Clifford-八元数分叉的Fano机制
——从逻辑奇点到物理代数的必然路径


---

# ═══ 0. 逻辑奇点 ═══

## §0 ∂∂ = 0

```
∂(∞|NULL) = S⁰ = {+1,−1}     ← 未区分→区分
∂(S⁰) → 0                    ← 对区分的区分为空（ker ≠ 0 ⟹ ∂不可逆）
```

有向有环：∞|NULL → S⁰ → 定理链 → 物理 → (无法回溯至∞|NULL)

二值逻辑（排中律）即公理选择，非定理。本文全部推导在此公理内。换公理→不同的数→不同的物理。公理链详见§2。


# ═══ I. 三重同一性：S⁰迭代 ≅ CD栈 ≅ F₂维度 ═══

## §1 凯莱-迪克森压栈与F₂指标空间

每次S⁰施加：CD维度×2，F₂向量空间维度+1。同一操作的三个范畴名。

| S⁰次数 | CD代数 | 维度 | F₂空间 | 射影空间 | 除法代数 |
|---|---|---|---|---|---|
| 0 | ℝ | 2⁰=1 | — | — | ✓ |
| 1 | ℂ | 2¹=2 | F₂¹ | PG(0,F₂)=点 | ✓ |
| 2 | ℍ | 2²=4 | F₂² | PG(1,F₂)=3点线 | ✓ |
| 3 | 𝕆 | 2³=8 | F₂³ | PG(2,F₂)=Fano | ✓（极限） |
| 4 | 𝕊 | 2⁴=16 | F₂⁴ | PG(3,F₂) | ✗（零因子） |

Frobenius定理：ℝ上结合除法代数仅 ℝ,ℂ,ℍ。Hurwitz定理：赋范除法代数中，放宽结合律仅增 𝕆。

三条独立路径（Hurwitz / Clifford-Bott周期 / E₈-Freudenthal魔方）交汇于8。E₈通过Freudenthal魔方下游于𝕆（$E_8 \leftrightarrow \mathbb{O} \otimes \mathbb{O}$）。公共根 $:=$ CD倍增（即S⁰迭代）。

## §2 Hurwitz截断

第4步零因子：区分产出不可区分 ⟹ 系统自截断。3非设计参数，为除法代数范畴内S⁰幂次的代数极限。

### 公理链

```
排中律（公理）⟹ ℝ完备有序域 ⟹ Artin-Schreier（代数闭包度 ∈ {1,2}）
  ⟹ [ℂ:ℝ]=2 ⟹ Frobenius（结合止于 ℍ=2²）⟹ Hurwitz（赋范止于 𝕆=2³）
```

每一环在ZFC内可证。首环（排中律）为公理。

"8是必然的" $:=$ "在二值逻辑为公理的体系内必然"。


# ═══ II. F₂³ → Fano唯一 ═══

## §3 维度阶梯与唯一性

| 射影空间 | 维度 | 处理能力 |
|---|---|---|
| PG(0,F₂) | 0D | 存在·无处理 |
| PG(1,F₂) | 1D | 传递·无分叉·度数≤2 |
| PG(2,F₂) | 2D | 首次分叉·每点度数3 |

底层向量空间维数公式：

$$\dim(U \cap V) = \dim(U) + \dim(V) - \dim(U+V) = 2 + 2 - 3 = 1$$

任意两个2D子空间（射影1D线）交于1D子空间（射影0D点）⟹ 射影平面公理。计数：7点7线，每线3点，每点3线。阶2射影平面同构唯一。

```
S⁰迭代 → Hurwitz截断于3 → F₂³ → 维数公式 → 任意两线必交 → Fano唯一
```


# ═══ III. Fano骨架与2-cocycle分叉 ═══

## §4 F₂加法骨架与符号选择

$Cl(3)$有 $2^3 = 8$ 基元素。7个非标量基 $\leftrightarrow \mathbb{F}_2^3 \setminus \{0\} \leftrightarrow$ Fano 7点。

### 乘法法则

$$e_x \cdot e_y = (-1)^{f(x,y)} \cdot e_{x \oplus y}, \quad x,y \in \mathbb{F}_2^3 \setminus \{0\}$$

e₀ := 1（标量·乘法恒等元·F₂³零向量）。

剥掉符号 $(-1)^f$ → 下标为F₂加法（XOR）。符号法则 $f: \mathbb{F}_2^3 \times \mathbb{F}_2^3 \to \mathbb{F}_2$ 即2-余循环。

- 2-cocycle选择A → $Cl(3)$（结合律成立）
- 2-cocycle选择B → $\mathbb{O}$（结合律破缺）
- 共享同一F₂加法骨架，区别仅在cocycle。

$G_2 = \text{Aut}(\mathbb{O})$（保cocycle+骨架）。$PSL(2,7) = \text{Aut(Fano)}$（只保骨架）。两群的精确范畴关系 ≠ 离散→连续（开放）。


# ═══ IV. 7 = 3+4：Fano退化与Triality ═══

## §5 射影→仿射退化

移除超平面 $L_\infty$（1条线 = 3点）→ 4个仿射点。$PSL(2,7)$传递 ⟹ 7条线全等价 ⟹ 切割结果唯一(up to同构)。S⁰不选择切哪条线。（几何等价性在§12被cocycle打破。）

## §6 实轴挂载与Triality

Fano提供7个虚生成元（$\text{Im}\,\mathbb{O} \cong \mathbb{R}^7$）。标量1补回 → $\mathbb{R}^8$。

### ℍ⊕ℍ⊥分解

任一Fano线（3点）+ 标量1 → span封闭于乘法 $\cong \mathbb{H}$（结合·除法代数）。剩余4点 $=: \mathbb{H}^\perp$，乘法不封闭：$\mathbb{H}^\perp \times \mathbb{H}^\perp \subset \mathbb{H}$（产物回落ℍ）。

$PSL(2,7)$传递 ⟹ $\mathbb{H} \oplus \mathbb{H}^\perp$ 分解唯一(up to同构)。ℍ即物质基座（封闭·自持），$\mathbb{H}^\perp$ 即力载体（作用后回落物质层）。

### 非结合律后果

- $Cl(3)$（结合）→ 左乘 $L_x$ 为同态 → 低维闭合
- $\mathbb{O}$（非结合）→ $L_x L_y \neq L_{xy}$ → 闭合需更多自由度
- $\mathbb{O}$虚单位左乘算符生成 $Cl(0,7) \cong Cl^0(0,8)$（偶子代数）→ $Spin(8)$
- $Cl(0,7)$唯一不可约表示作用于 $\mathbb{R}^8$

### Triality

$Spin(8)$三个8D不可约表示：$V_8$（向量）/ $S_8^+$ / $S_8^-$（旋量）。$\mathbb{O}$乘法 $\mathbb{O} \times \mathbb{O} \to \mathbb{O}$ 中三份$\mathbb{O}$在Triality下分别对应 $V_8, S_8^+, S_8^-$。

唯一三线性不变量 $\Phi(v, s_1, s_2) \leftrightarrow$ 八元数乘法 $\leftrightarrow$ Fano线3点。1力($V_8$) + 2物质($S_8^+, S_8^-$)为唯一产出标量的组合。

群论唯一性：$Out(Spin(8)) \cong S_3$，所有单李群中绝对孤立。此 $S_3$ 允许 $V_8, S_8^+, S_8^-$ 三者轮换，锁死顶点结构为1规范向量+2手征旋量。


# ═══ V. 84：双路径交汇 ═══

## §7 两条独立路径

**路径一（G₂）：**

$G_2 = \text{Aut}(\mathbb{O})$，rank 2，12根，dim 14。$12 \times \dim(\text{Im}\,\mathbb{O}) = 12 \times 7 = 84$。84对应的精确数学对象开放。

**路径二（Hurwitz界）：**

$(2,3,7)$三角群，基本域面积 $\pi/42$，含反射 $\pi/84$。Hurwitz界：$g \geq 2$ 紧黎曼面自同构群阶 $\leq 84(g-1)$。Klein四次曲线（$g = 3$）饱和此界：$|\text{Aut}| = 168 = 84 \times 2$。

$$\text{Aut(Klein quartic)} \cong PSL(2,7) \cong GL(3, \mathbb{F}_2) = \text{Aut(Fano)}$$

公共根 $:=$ Fano。$|PSL(2,7)| = 168 = 2 \times 84$。$\mathbb{F}_2 \cong S^0$。

## §8 SAW第五项

$t_5 := t_{4b} / 84$。$84 = |PSL(2,7)| / 2$（Fano组合阶数，取定向）。

$$\mu_{5项} = 4.684039932903 \quad \text{vs} \quad \mu_{\text{Clisby}} = 4.684039931 \pm 27 \times 10^{-9} \quad \text{残差 } 0.07\sigma$$

纤维丛 $SU(2) \hookrightarrow G_2 \twoheadrightarrow G_2/SU(2) \cong V_2(\mathbb{R}^7)$（arXiv:2411.18881）：底空间为 $\mathbb{R}^7$ 中正交2-标架的Stiefel流形，纤维 $\cong SU(2)$。SAW转向抉择的内部自由度（零因子配对）与纤维$SU(2)$关联，$30^2 = SU(2)$握手空间。

数值详见附录C。


# ═══ VI. SC连通性 → Fano涌现 ═══

## §9 SC闭邻域 ≅ F₂³

SC配位数 $Z = 6$。闭邻域 $N[v_0] = \{v_0, x_+, x_-, y_+, y_-, z_+, z_-\}$，共7点。7个非零状态需最少3个F₂坐标（$2^3 - 1 = 7$）⟹ $\mathbb{F}_2^3$ 为唯一合法标签空间。

正方向锚定：$x_+ \mapsto e_1 = (100)$，$y_+ \mapsto e_2 = (010)$，$z_+ \mapsto e_3 = (001)$。轴共线约束 $a \oplus b \oplus c = 0$ + $v_0$穷举（4候选，3与已有标签冲突）⟹ $v_0 = (111)$ 唯一无冲突。

| SC位置 | F₂³ | SC位置 | F₂³ |
|---|---|---|---|
| x₊ | (100) | x₋ | (011) |
| y₊ | (010) | y₋ | (101) |
| z₊ | (001) | z₋ | (110) |
| v₀ | (111) | | |

$GL(3, \mathbb{F}_2) \cong PSL(2,7)$：正方向选取 $\cong$ Fano自同构，关联结构不变。

```
SC图(拓扑) → 闭邻域7点 → F₂³(唯一) → 轴约束 → v₀(唯一) → Fano涌现
每步零自由度。
```

穷举验证详见附录A。

## §10 SC结构

### 引力 ≡ 连通性

SC每边 = 1次S⁰ = ln 2 bit。规范力经cocycle筛选（§12: $1/2 \times 1/3 = 1/6$），只用部分边。引力用全部边，不经筛选。

导出顺序即本体论顺序：

```
连通性(引力) ⊃ F₂³局部代数(Fano) ⊃ cocycle筛选(规范力)
```

引力弱：Fano关联密度为1，SC展开后邻接比 $6/N \to 0$。引力无需量子化：图的连通性质，非场的激发。

### Hamming[7,4,3] ≅ Fano

校验矩阵H的7列 $\leftrightarrow$ Fano 7点。$\ker(H)$ = 合法配置。$\text{rank}(H) = 3$（结构）$+ \text{nullity}(H) = 4$（数据）$= 7$。

Hamming 3+4（生成元/复合元）≠ 仿射 3+4（$L_\infty$/仿射点）：

- Hamming基 $\{(100),(010),(001)\}$ 线性无关（不共线·三角形散布全局）
- $L_\infty$ 上3点满足 $a \oplus b \oplus c = 0$，线性相关（共线·封闭子空间）
- 线性无关 vs 线性相关 ⟹ 两种3+4代数正交，不可重叠

Hamming不共线 → 3轴隔离（校验代价）。$L_\infty$共线 → 物质基座封闭（ℍ子代数）。

### F₂ vs ℤ：同一生成元，两种记账

| | F₂ | ℤ |
|---|---|---|
| 运算结果 | Fano（有限·7态·1⊕1=0） | SC晶格（无限·ℤ³·1+1=2） |
| 历史 | 不记录 | 记录 |

分叉判据为序：F₂模2截断因果来源（真随机源），ℤ因果链单调。$\ln 2$ 为F₂/ℤ换算常数：每次S⁰在F₂侧翻1 bit，在ℤ侧累加1步，产出 $= \ln 2$ nat。

因果方向：$\mathbb{F}_2^3 \cong \mathbb{Z}^3 / 2\mathbb{Z}^3$（mod 2商群投影，NULL约束∞），非ℤ³展开F₂³。Fano / SC / 引力对应同一 $\mathbb{F}_2^3$ 的三个投影（代数/空间/连通性），$\ln 2$ 全投影守恒。

### F₂³的五重独立分解

| 分解 | 来源 |
|---|---|
| 8 = 7+1 | F₂³ = Fano + 零向量（射影排除恒等元） |
| 7 = 1+6 | SC配位（锚点 = 局部化零向量 + 邻居） |
| 7 = 3+4 | Hamming（rank+nullity） |
| 7 = 3+4 | Fano退化（L∞+仿射） |
| 6 = 3+3 | cocycle（正+负） |

五层嵌套：8→7→(两个独立3+4)→6→3+3。零向量在每层换身份但不消失：F₂³加法恒等元 → 𝕆实单位e₀=1 → SC锚点 → 3D坐标原点。结构必需但射影不可见。物理可观测量出现在约束交集处。


# ═══ VII. Cocycle S₄破缺与规范力 ═══

## §11 Cocycle 7 = 4+3

Cartan-Schouten定向，7个三元组：

$(1,2,3),\ (1,4,5),\ (2,4,6),\ (2,5,7),\ (1,6,7),\ (3,4,7),\ (3,5,6)$

$e_a \cdot e_b = +e_{a \oplus b}$（顺）或 $-e_{a \oplus b}$（逆）。

**标签切换**：§9的 $e_k$ 为 $\mathbb{F}_2^3$ 基底，本节起 $e_k$ 为𝕆虚单位（1–7）。

| 𝕆单位 | F₂³ | SC位置 |
|---|---|---|
| e₁ | (100) | x₊ |
| e₂ | (010) | y₊ |
| e₃ | (110) | z₋ |
| e₄ | (001) | z₊ |
| e₅ | (101) | y₋ |
| e₆ | (011) | x₋ |
| e₇ | (111) | v₀ |

Cocycle将7点分为（全表详见附录B）：

- **线正集**（+集构成Fano线）：$\{e_1, e_3, e_5, e_7\}$ = $\mathbb{F}_2^3$ 第一坐标为1
- **非线正集**（+集不构成线）：$\{e_2, e_4, e_6\}$ = 第一坐标为0，本身构成线

$7 = 4+3$ 为 $\mathbb{F}_2^3$ 第一坐标的值。Cocycle打破 $PSL(2,7)$ 对称性。

## §12 唯一1+3线与S₄正向邻接度

对7条线分别做 $L_\infty$，计算仿射点正向邻接度 $\chi(p)$（正邻居落在 $L_\infty$ 中的个数）：

| L∞ | χ分布 | 类型 |
|---|---|---|
| {1,2,3} | (0,1,2,3) | 全不同 |
| {1,4,5} | (2,3,0,1) | 全不同 |
| {1,6,7} | (0,1,2,3) | 全不同 |
| **{2,4,6}** | **(3,1,1,1)** | **1+3** |
| {2,5,7} | (1,1,2,2) | 2+2 |
| {3,4,7} | (1,2,1,2) | 2+2 |
| {3,5,6} | (1,2,2,1) | 2+2 |

过 $e_1$ 的3条 → 全不同。$\{2,4,6\}$ → 唯一1+3。其余 → 2+2。

L∞ = {e₂,e₄,e₆} 时仿射正向邻接度：

| 锚 | 正集 | ∩L∞ | χ |
|---|---|---|---|
| e₁ | {2,4,6} | 全部 | **3 (singlet)** |
| e₃ | {1,4,5} | {4} | 1 |
| e₅ | {1,6,7} | {6} | 1 |
| e₇ | {1,2,3} | {2} | 1 |

$\chi(e_1) = 3$（全相干·singlet）。$\chi(e_3) = \chi(e_5) = \chi(e_7) = 1$（弱相干·triplet，三者等价）。

§5修正：$PSL(2,7)$传递保证几何等价，cocycle打破之。$L_\infty$ 非任意切割。Cocycle选出唯一 $L_\infty = \{e_2, e_4, e_6\}$ = 物质基座($\mathbb{H}$)。

### 1/6筛选

$$\frac{1}{2} \times \frac{1}{3} = \frac{1}{6}$$

cocycle方向（3正3负）× 线选择（每锚3条取1）。

$U(1)$可见邻居 $:=$ 正向 $\wedge$ 在 $e_1$ 所属线上，必然1个。

## §13 U(1) × SU(3) × SU(2)

### U(1) ← singlet $e_1$

CD分解：$\mathbb{O} = \mathbb{H} \oplus \mathbb{H}e_1$。$\mathbb{C} = \text{span}(1, e_1)$。$e_1$ 对物质层做全局相位旋转，不改变粒子身份。

### SU(3) ← $\text{Stab}_{G_2}(e_1)$

Günaydin-Gürsey：$\text{Stab}_{G_2}(e_1) \cong SU(3)$。$e_1$ 充当剩余6维空间的复结构 $J(x) := e_1 \cdot x$（$J^2 = -\text{Id}$）。6虚单位配成3复坐标 $C_k = (\text{物质}\, e_k,\ \text{力}\, e_m)$，$e_m = e_1 \oplus e_k$：

| Cₖ | 物质(L∞) | 力(triplet) | 关系 |
|---|---|---|---|
| C₁ | e₂ | e₃ = e₁⊕e₂ | 力 = e₁⊕物质 |
| C₂ | e₄ | e₅ = e₁⊕e₄ | 力 = e₁⊕物质 |
| C₃ | e₆ | e₇ = e₁⊕e₆ | 力 = e₁⊕物质 |

Triplet $\{e_3, e_5, e_7\}$ 为物质的"颜色"坐标轴。3复坐标间保范变换即 $SU(3)$"色"对称。

### SU(2) ← $L_\infty$ 四元数结构

$$[e_2, e_4] = 2e_6, \quad [e_4, e_6] = 2e_2, \quad [e_6, e_2] = 2e_4 \quad \leftarrow\; \mathfrak{su}(2)$$

$SU(2) \leftrightarrow \mathbb{H}$内禀 $\mathfrak{su}(2)$ 的指数映射（局部→整体）。

### Triplet溢出

$$e_3 \cdot e_5 = +e_6 \in L_\infty, \quad e_5 \cdot e_7 = +e_2 \in L_\infty, \quad e_3 \cdot e_7 = -e_4 \in L_\infty$$

Triplet自乘全落入 $L_\infty$：力→物质代数耦合通道，非$SU(2)$本身。

### 导出层级（背景→前景）

```
引力(SC全边) →[局部代数] SU(2)(L∞·ℍ) →[固定e₁] U(1)(e₁·ℂ) →[稳定子] SU(3)(triplet·"色")
```

箭头为导出顺序，非群包含（群论上 $SU(3) \supset SU(2) \supset U(1)$，与导出序反向）。


# ═══ VIII. 带宽不等式 ═══

## §14 Fano最大匹配数 $\nu = 1$

最大匹配数 $\nu$ 即互不相交超边的最大集合基数，等价于系统并发处理能力。

- **线模型**（操作 $:=$ 乘法，冲突 $:=$ 共享点）：射影公理（任意两线必交）⟹ $\nu = 1$
- **共线模型**（操作 $:=$ 翻转，冲突 $:=$ 共线）：任意两点共线 ⟹ $\nu = 1$

SC对比：$N$顶点，$\nu = O(N)$。Fano $\nu \equiv 1$ vs SC $\nu = O(N)$。3D空间即系统为打破 $\nu = 1$ 而开辟的空间多路复用。

## §15 $N > A$：信息溢出

产出端：$N$个活跃Fano节点，每节点每 $t_P$ 处理1次S⁰ $= \ln 2$ bit。$\Gamma_{\text{prod}} = N \cdot \ln 2 / t_P$。

边界端：Bekenstein-Hawking熵界——体积内最大信息状态数由表面积 $A$（Planck面积计）卡死。$\Gamma_{\text{bound}} = A \cdot \ln 2 / t_P$。

$\ln 2 / t_P$ 消去，纯组合不等式：

$N > A$ 即信息溢出：内部自由度超越边界全息极限。

球形（半径 $R$ Planck单位；连续近似与SC离散等周不等式同阶）：

$$N \sim \frac{4}{3}\pi R^3, \quad A \sim 4\pi R^2, \quad N/A = R/3$$

溢出条件：$R > 3$。$R \geq 3$ 起，信息产出永久超过边界容量。

### N>A的三种响应（且仅三种）

1. 冻结节点（N↓）→ 无处理自由度 → **物质**
2. 扩大表面（A↑）→ 空间膨胀 → **暗能量**
3. 两者共存 → **我们的宇宙**

$N \sim R^3$ 增长永远快于 $A \sim R^2$ ⟹ 永续加速膨胀。信息结构后果，无需真空能。

### 连接

- $R = 3 \leftrightarrow$ Hamming rank $= 3 \leftrightarrow \mathbb{F}_2^3$ 维度
- 表面容量 $\propto R^2 \leftrightarrow$ Bekenstein-Hawking熵界（全息原理，非黑洞特有）
- 每S⁰ $= \ln 2 =$ Landauer最小值 $\to k_B T \cdot \ln 2 \to E_P \cdot \ln 2$

### 信息循环

```
产出 →[N>A]→ 饱和 →[冻结]→ 物质 →[~10⁷⁰yr]→ Hawking辐射 → 回归
                  →[膨胀]→ A↑ → 缓解 →[N↑更快]→ 再饱和 → 永续
```


# ═══ 映射总表 ═══

| Fano结构 | 物理 | S⁰串联对应 |
|---|---|---|
| 7点 = F₂³∖{0} | 𝕆虚单位·Im𝕆 ≅ ℝ⁷ | I.CD栈 |
| 7线 | 𝕆乘法三元组 | — |
| L∞ = {e₂,e₄,e₆} | 物质基座ℍ·SU(2) | H³ |
| e₁ (singlet) | U(1)·电磁 | H² |
| {e₃,e₅,e₇} (triplet) | SU(3)·"色" | H¹ |
| SC全边 | 引力（父结构） | H⁰ |
| cocycle 7=4+3 | 力/物质分工 | — |
| 1/6 = 1/2×1/3 | 可见物质比例 | — |
| Triality V₈/S₈⁺/S₈⁻ | 1力+2物质 | — |
| Fano O(1) → SC O(N) | 3D即并行性物理形式 | §3维度阶梯 |
| N>A (R>3) | 物质+膨胀 | — |
| 84 = |PSL(2,7)|/2 | SAW t₅ | — |
| F₂/ℤ·ln2 | 双投影守恒 | II.ln2测度 |
| ℍ⊕ℍ⊥ | 物质(封闭)+力(回落) | 力/物质分工根因 |

导出顺序即本体论顺序：

```
∂∂=0 → S⁰ → F₂³ → Fano → cocycle → {U(1), SU(3), SU(2)} → 物理
连通性(引力) ⊃ Fano(局部代数) ⊃ cocycle(规范力)
```

协助作者（排名不分先后）：**Claude**、**Gemini**
形式设计：**Gemini**



# ═══ 附录 ═══

## 附录A v₀穷举与Fano线验证

v₀候选穷举（§9）：

| v₀ | x₋ = e₁⊕v₀ | 冲突检验 |
|---|---|---|
| (110) | (010) = y₊ | ✗ |
| (101) | (001) = z₊ | ✗ |
| (011) | y₋ = e₂⊕v₀ = (001) = z₊ | ✗ |
| (111) | (011)✓, y₋=(101)✓, z₋=(110)✓ | ✓ |

7线闭合验证：

**主轴：**$(100) \oplus (011) \oplus (111) = 0$，$(010) \oplus (101) \oplus (111) = 0$，$(001) \oplus (110) \oplus (111) = 0$

**仿射：**$(100) \oplus (010) \oplus (110) = 0$，$(100) \oplus (001) \oplus (101) = 0$，$(010) \oplus (001) \oplus (011) = 0$，$(110) \oplus (101) \oplus (011) = 0$

## 附录B Cocycle全表

每锚点3正+3负邻居（§11）：

| 锚 | 正集(+) | 负集(−) | +集∈线？ |
|---|---|---|---|
| e₁ | {2,4,6} | {3,5,7} | ✓ {e₂,e₄,e₆} |
| e₂ | {3,4,5} | {1,6,7} | ✗ |
| e₃ | {1,4,5} | {2,6,7} | ✓ {e₁,e₄,e₅} |
| e₄ | {5,6,7} | {1,2,3} | ✗ |
| e₅ | {1,6,7} | {2,3,4} | ✓ {e₁,e₆,e₇} |
| e₆ | {2,3,7} | {1,4,5} | ✗ |
| e₇ | {1,2,3} | {4,5,6} | ✓ {e₁,e₂,e₃} |

## 附录C SAW数值

完整级数：

```
μ = 5 - 1/π + 1/(136π) + 12/(136×900×π²) - 12/(16×136×900×π²) - 12/(84×16×136×900×π²)
```

| 项 | 公式 | 物理 |
|---|---|---|
| t₁ | 5 | 平均场（Z-1=5） |
| t₂ | -1/π | H¹闭合修正 |
| t₃ | 1/(136π) | 离散化税（α⁻¹=136） |
| t₄ₐ | 12/(136×900×π²) | 打结修正（900=30²） |
| t₄ᵦ | -12/(16×136×900×π²) | 博罗梅奥退税（16=SU(2)握手） |
| t₅ | -12/(84×16×136×900×π²) | Fano（84=|PSL(2,7)|/2） |

共享常数：5(Z-1), π(H¹闭合), 136(α⁻¹), 900=30²(SU(2)握手空间), 16(博罗梅奥), 84(Fano取定向)。一个错全错。


μ₅項 = 4.684039932903
μ_Clisby(2010) = 4.684039931 ± 0.000000027

残差：+1.903 ppb（0.07σ）。四项 0.34σ → 五项 0.07σ。

公式推导详见：区分的常性与常数§A.8。
